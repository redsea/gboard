/**
 * URL 데이터
 */

var gboard;
if(!gboard) { gboard = {}; }
if(!gboard.board) { gboard.board = {}; }
if(!gboard.board.url) { gboard.board.url={

document_list1:		'http://gboard.org/board/document_list1'	// document list type 1
	
};}

